// reducers.
import {
    RoomState,
} from '../state/index';
import {
    Action,
} from '../action/index';

export function roomReducer(state: RoomState, _action: Action): RoomState{
    return state;
}
